# Dependencies
node_modules/

# AIDesigner internal runs (keep only the deliverable)
.aidesigner/*
!.aidesigner/mcp-latest.html

# Claude Code internals
.claude/
.agents/
.mcp.json

# Large binary not needed in repo
*.pdf
